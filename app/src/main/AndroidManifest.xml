<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.style.probro">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/ThemeProBro">
        <activity android:name=".order.MyOrderDetailActivity"></activity>
        <activity android:name=".order.MyOrdersListActivity" />
        <activity android:name=".order.OrderSummaryActivity" />
        <activity android:name=".cart.MyCartActivity" />
        <activity android:name=".product_description.ProductDescriptionActivity" />
        <activity android:name=".auth.FirebaseUIActivity" />
        <activity
            android:name=".dashboard.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".termsandcondition.PrivacyPolicyActivity" />
        <activity android:name=".returnandrefund.ReturnPolicyActivity" />
        <activity
            android:name=".welcome.StartUpActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>